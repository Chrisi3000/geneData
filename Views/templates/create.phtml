<?php include __DIR__ . "/_header.phtml"; ?>

    <a href="/geneData/GeneDataItem"
       class="btn btn-outline-success rounded-pill px-3">
        &#8592; Back
    </a>

    <h2>Add Gene</h2>

    <form method="post" action="/geneData/GeneDataItem/post" class="needs-validation was-validated" novalidate>

        <div class="mb-3">
            <label class="form-label">Gene Name</label>
            <input type="text" name="genename" class="form-control" minlength="2" maxlength="255" required>
            <div class="invalid-feedback">please enter a valid gene name (min 2 characters).</div>
        </div>

        <div class="mb-3">
            <label class="form-label">Gene Symbol</label>
            <input type="text" name="genesymbol" class="form-control" pattern="[A-Za-z0-9\-]{2,20}" required>
            <div class="invalid-feedback">please enter a valid symbol (2-20 alphanumeric characters or hyphens).</div>
        </div>

        <div class="mb-3">
            <label class="form-label">Aliases</label>
            <input type="text" name="aliases" class="form-control" maxlength="255">
        </div>

        <div class="mb-3">
            <label class="form-label">Position</label>
            <input type="text" name="position" class="form-control" placeholder="e.g., Chr1:1000-2000" minlength="3" maxlength="100" required>
            <div class="invalid-feedback">please specify the chromosomal position profile mapping.</div>
        </div>

        <div class="mb-3">
            <label class="form-label">Function</label>
            <textarea name="function" class="form-control" maxlength="500"></textarea>
        </div>

        <div class="mb-3">
            <label class="form-label">Organism</label>
            <select name="organism_id" class="form-select" required>
                <option value="" disabled selected>-- please choose --</option>
                <?php foreach ($data as $organism): ?>
                    <option value="<?= (int)$organism->id ?>">
                        <?= htmlspecialchars($organism->name) ?>
                        <?php if (!empty($organism->latin_name)): ?>
                            (<?= htmlspecialchars($organism->latin_name) ?>)
                        <?php endif; ?>
                    </option>
                <?php endforeach; ?>
            </select>
            <div class="invalid-feedback">please select a target host organism sequence.</div>
        </div>

        <div class="mb-3 form-check">
            <input type="checkbox" name="reviewed" value="1" class="form-check-input" id="reviewed">
            <label class="form-check-label" for="reviewed">Reviewed</label>
        </div>

        <button type="submit" class="btn btn-primary">
            Save
        </button>

    </form>

    <script>
        // intercepts fallback submission procedures to cancel execution if elements fail constraints
        document.addEventListener('DOMContentLoaded', () => {
            const form = document.querySelector('.needs-validation');
            form.addEventListener('submit', (event) => {
                if (!form.checkValidity()) {
                    event.preventDefault();
                    event.stopPropagation();
                }
            }, false);
        });
    </script>

<?php include __DIR__ . "/_footer.phtml"; ?>
