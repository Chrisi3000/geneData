<?php include __DIR__."/_header.phtml"; ?>

<?php /** @var array $data */ ?>
<?php $escape = fn($value) => htmlspecialchars((string)$value, ENT_QUOTES, "UTF-8"); ?>

<?php if ($data) { ?>
    <table class="table table-striped table-hover">
        <thead>
        <tr>
            <?php
            foreach($data[0]->property_names() as $key) {
                ?>
                <th><?=$escape($key)?></th>
                <?php
            }
            ?>
        </tr>
        </thead>
        <tbody>
        <?php
        foreach ($data as $obj) {
            ?>
            <tr>
                <?php
                foreach($obj->property_names() as $key) {
                    ?>
                    <td><?=$escape($obj->$key)?></td>
                    <?php
                }
                ?>
            </tr>
            <?php
        }
        ?>
        </tbody>
    </table>
<?php } else { ?>
    <div>No data found.</div>
<?php } ?>
<?php include __DIR__."/_footer.phtml"; ?>
